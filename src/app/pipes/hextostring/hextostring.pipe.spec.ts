import { HextostringPipe } from './hextostring.pipe';

describe('HextostringPipe', () => {
  it('create an instance', () => {
    const pipe = new HextostringPipe();
    expect(pipe).toBeTruthy();
  });
});
